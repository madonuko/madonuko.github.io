remote_theme: jekyll/minima
plugins:
  - jekyll-remote-theme
  - jekyll-feed
minima:
  skin: dark
title: "a_cat.on(⌨️)"
description: Personal blog in a paper box
defaults:
  - scope:
      path: "_posts"
    values:
      layout: "posts"
